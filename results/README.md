# Results

Commit small, reviewed CSV/JSON summaries here when they support an experiment
note. Put large or disposable output under `results/raw/`, which is ignored.
Never compare results across machines without preserving environment metadata.

