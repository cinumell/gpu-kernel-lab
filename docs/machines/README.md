# Machine records

Run `python scripts/capture_env.py > docs/machines/<hostname>-<date>.txt` once per
environment change. Review the output before committing: hostnames and paths can
be sensitive on work machines. Add power limits, application clocks, and MIG
configuration manually when relevant.

